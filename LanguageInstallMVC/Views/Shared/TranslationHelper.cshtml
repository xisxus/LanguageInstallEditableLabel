@using LanguageInstall.Service 
@inject LanguageInstall.Service.Service.ILocalizationService TranslationService 

@functions {
    public async Task<string> TranslateAsync(string code ,string key, string languageCode)
    {
        return await TranslationService.GetTranslationAsync(code , key, languageCode);
    }
}
